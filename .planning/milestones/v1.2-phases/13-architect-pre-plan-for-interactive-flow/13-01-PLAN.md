---
phase: 13-architect-pre-plan-for-interactive-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/architect_pre_plan.py
  - backend/utils/debug_logger.py
  - backend/utils/settings_store.py
  - backend/tests/test_agents_mock.py
autonomous: true
requirements: [RTE-01, RTE-05]

must_haves:
  truths:
    - "ArchitectPrePlanAgent.run() returns a dict with 'regions' list and 'total_nights' integer"
    - "Each region has 'name', 'recommended_nights', and 'max_drive_hours' fields"
    - "The agent prompt enforces total nights = total_days - 1 and max_drive_hours_per_day between regions"
    - "Nights distribution is based on destination potential, not always minimum (per D-04, D-05, RTE-05)"
    - "Agent uses Sonnet model and follows established agent pattern (D-07, D-08)"
  artifacts:
    - path: "backend/agents/architect_pre_plan.py"
      provides: "ArchitectPrePlanAgent class"
      exports: ["ArchitectPrePlanAgent"]
      contains: "class ArchitectPrePlanAgent"
    - path: "backend/utils/debug_logger.py"
      provides: "Log routing for new agent"
      contains: "ArchitectPrePlan"
    - path: "backend/utils/settings_store.py"
      provides: "Default model and max_tokens for new agent"
      contains: "agent.architect_pre_plan.model"
    - path: "backend/tests/test_agents_mock.py"
      provides: "Unit tests for agent run() and prompt content"
  key_links:
    - from: "backend/agents/architect_pre_plan.py"
      to: "backend/agents/_client.py"
      via: "get_client(), get_model(), get_max_tokens()"
      pattern: "get_model.*claude-sonnet-4-5.*architect_pre_plan"
    - from: "backend/agents/architect_pre_plan.py"
      to: "backend/utils/retry_helper.py"
      via: "call_with_retry()"
      pattern: "call_with_retry"
---

<objective>
Create the ArchitectPrePlanAgent that generates a lightweight region plan with recommended nights per region based on destination potential. Register the agent in debug_logger and settings_store.

Purpose: RTE-01 requires a pre-plan before stop selection; RTE-05 requires nights based on place potential rather than always minimum. This plan creates the agent and its tests.
Output: New agent file, updated registrations, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-architect-pre-plan-for-interactive-flow/13-CONTEXT.md
@.planning/phases/13-architect-pre-plan-for-interactive-flow/13-RESEARCH.md

@backend/agents/route_architect.py
@backend/agents/_client.py
@backend/utils/debug_logger.py
@backend/utils/settings_store.py
@backend/utils/retry_helper.py
@backend/utils/json_parser.py
@backend/models/travel_request.py

<interfaces>
From backend/agents/_client.py:
```python
def get_client() -> anthropic.Anthropic
def get_model(default_model: str, agent_key: str) -> str
def get_max_tokens(agent_key: str, default: int) -> int
```

From backend/utils/retry_helper.py:
```python
async def call_with_retry(fn, job_id: str = "", agent_name: str = "", max_attempts: int = 5) -> Any
```

From backend/utils/json_parser.py:
```python
def parse_agent_json(text: str) -> dict
```

From backend/utils/debug_logger.py:
```python
class LogLevel(str, Enum): DEBUG, INFO, SUCCESS, WARNING, ERROR, AGENT, API, PROMPT
async def log(level, message, job_id=None, agent=None)
async def log_prompt(agent, model, prompt, job_id=None)
```

From backend/models/travel_request.py:
```python
class TravelRequest(BaseModel):
    legs: List[TripLeg]
    travel_styles: List[str] = []
    travel_description: str = ""
    preferred_activities: List[str] = []
    mandatory_activities: List[MandatoryActivity] = []
    max_drive_hours_per_day: float = 4.5
    # ... (total_days is computed per-leg as leg.total_days)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ArchitectPrePlanAgent with tests</name>
  <files>backend/agents/architect_pre_plan.py, backend/tests/test_agents_mock.py</files>
  <read_first>
    - backend/agents/route_architect.py (reference agent pattern to copy structure)
    - backend/agents/_client.py (get_client, get_model, get_max_tokens signatures)
    - backend/utils/retry_helper.py (call_with_retry signature)
    - backend/utils/json_parser.py (parse_agent_json)
    - backend/models/travel_request.py (TravelRequest fields: travel_description, travel_styles, preferred_activities, mandatory_activities, max_drive_hours_per_day, legs)
    - backend/models/trip_leg.py (TripLeg: start_location, end_location, total_days property)
    - backend/tests/test_agents_mock.py (existing test structure, _make_single_transit_req helper)
  </read_first>
  <behavior>
    - Test 1 (test_architect_pre_plan_agent): Mock Anthropic client to return JSON `{"regions": [{"name": "Provence", "recommended_nights": 3, "max_drive_hours": 3.0}, {"name": "Paris", "recommended_nights": 6, "max_drive_hours": 4.0}], "total_nights": 9}`. Call `agent.run()`. Assert result has "regions" key with list of 2 items. Assert each region has "name", "recommended_nights", "max_drive_hours".
    - Test 2 (test_architect_pre_plan_prompt_includes_context): Create request with travel_description="Kulturreise", travel_styles=["Kultur", "Kulinarik"], preferred_activities=["Museen"]. Call `agent._build_prompt()`. Assert prompt contains "Kulturreise", "Kultur", "Kulinarik", "Museen", the leg's start_location, end_location, and nights budget (total_days - 1).
    - Test 3 (test_architect_pre_plan_prompt_drive_limit): Create request with max_drive_hours_per_day=3.5. Call `agent._build_prompt()`. Assert prompt contains "3.5" (the drive limit value per D-03).
    - Test 4 (test_architect_pre_plan_nights_budget): Create request with 10-day leg. Call `agent._build_prompt()`. Assert prompt contains "9" as the nights budget (total_days - 1 per D-06).
  </behavior>
  <action>
    **First write failing tests in `backend/tests/test_agents_mock.py`**, then create the agent.

    Add 4 test functions at the end of test_agents_mock.py. Use `_make_single_transit_req()` helper for request creation. Mock `anthropic.Anthropic` the same way existing agent tests do (e.g., `test_route_architect_agent` pattern).

    Then create `backend/agents/architect_pre_plan.py` with:

    1. `AGENT_KEY = "architect_pre_plan"`
    2. `SYSTEM_PROMPT` in German (per D-08) — see RESEARCH.md "Recommended System Prompt" section for exact wording. Key constraints in prompt:
       - Nights nach Potential verteilen (RTE-05, D-04)
       - Kein Regionswechsel darf max_drive_hours_per_day ueberschreiten (D-03)
       - Summe Naechte = Naechtebudget (D-06)
       - JSON-only output
    3. `class ArchitectPrePlanAgent` with:
       - `__init__(self, request: TravelRequest, job_id: str)` — sets self.request, self.job_id, self.client via get_client(), self.model via get_model("claude-sonnet-4-5", AGENT_KEY)
       - `_build_prompt(self) -> str` — builds prompt from `self.request.legs[0]` (pre-plan is first-leg only). Includes:
         - `leg.start_location`, `leg.end_location`
         - nights_budget = `leg.total_days - 1`
         - `req.max_drive_hours_per_day` (D-03)
         - `req.travel_styles` joined as comma-separated string, default "allgemein"
         - `req.travel_description` if non-empty (conditional line per Phase 12 pattern)
         - `req.preferred_activities` if non-empty (conditional line)
         - `req.mandatory_activities` if non-empty — format: name only, joined (conditional line)
         - JSON schema example with `regions` array and `total_nights`
         - Rules block: sum constraint, drive limit, geographic logic, potential-based distribution
       - `async def run(self) -> dict` — logs API call with `debug_logger.log(LogLevel.API, ...)` and prompt with `debug_logger.log_prompt(...)`, wraps `self.client.messages.create()` in `call_with_retry()`, parses with `parse_agent_json()`, returns dict.

    Use `agent="ArchitectPrePlan"` for all debug_logger calls (matches _COMPONENT_MAP key to be added in Task 2).

    max_tokens: `get_max_tokens(AGENT_KEY, 1024)` — 1024 default is sufficient for lean pre-plan output (D-02).
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/Travelman3/backend && python3 -m pytest tests/test_agents_mock.py::test_architect_pre_plan_agent tests/test_agents_mock.py::test_architect_pre_plan_prompt_includes_context tests/test_agents_mock.py::test_architect_pre_plan_prompt_drive_limit tests/test_agents_mock.py::test_architect_pre_plan_nights_budget -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/agents/architect_pre_plan.py exists and contains `class ArchitectPrePlanAgent`
    - backend/agents/architect_pre_plan.py contains `AGENT_KEY = "architect_pre_plan"`
    - backend/agents/architect_pre_plan.py contains `get_model("claude-sonnet-4-5"` (Sonnet per D-07)
    - backend/agents/architect_pre_plan.py contains `call_with_retry(` and `parse_agent_json(`
    - backend/agents/architect_pre_plan.py _build_prompt contains `max_drive_hours_per_day` (D-03)
    - backend/agents/architect_pre_plan.py _build_prompt contains `total_days - 1` or equivalent nights budget calculation (D-06)
    - backend/agents/architect_pre_plan.py _build_prompt contains `travel_description` conditional (D-04)
    - backend/agents/architect_pre_plan.py _build_prompt contains `travel_styles` (D-04)
    - backend/tests/test_agents_mock.py contains `test_architect_pre_plan_agent`
    - backend/tests/test_agents_mock.py contains `test_architect_pre_plan_prompt_includes_context`
    - All 4 new tests pass
  </acceptance_criteria>
  <done>ArchitectPrePlanAgent created with German system prompt, potential-based nights distribution in prompt, drive limit enforcement, and 4 passing unit tests</done>
</task>

<task type="auto">
  <name>Task 2: Register agent in debug_logger and settings_store</name>
  <files>backend/utils/debug_logger.py, backend/utils/settings_store.py</files>
  <read_first>
    - backend/utils/debug_logger.py (current _COMPONENT_MAP entries around lines 42-60)
    - backend/utils/settings_store.py (current DEFAULTS dict lines 18-61, _RANGES dict lines 67-89)
  </read_first>
  <action>
    1. In `backend/utils/debug_logger.py`, add two entries to `_COMPONENT_MAP` dict (following the existing dual-name pattern):
       ```python
       "ArchitectPrePlan": "agents/architect_pre_plan",
       "ArchitectPrePlanAgent": "agents/architect_pre_plan",
       ```
       Place after the existing `RouteArchitectAgent` entries (alphabetical grouping).

    2. In `backend/utils/settings_store.py`, add to `DEFAULTS` dict in the `Agent-Modelle` section:
       ```python
       "agent.architect_pre_plan.model": "claude-sonnet-4-5",
       ```
       And in the `Agent max_tokens` section:
       ```python
       "agent.architect_pre_plan.max_tokens": 1024,
       ```

    3. In `backend/utils/settings_store.py`, add to `_RANGES` dict:
       ```python
       "agent.architect_pre_plan.max_tokens": (512, 4096),
       ```
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/Travelman3/backend && python3 -c "from utils.debug_logger import _COMPONENT_MAP; assert 'ArchitectPrePlan' in _COMPONENT_MAP; assert 'ArchitectPrePlanAgent' in _COMPONENT_MAP; print('debug_logger OK')" && python3 -c "from utils.settings_store import DEFAULTS, _RANGES; assert DEFAULTS['agent.architect_pre_plan.model'] == 'claude-sonnet-4-5'; assert DEFAULTS['agent.architect_pre_plan.max_tokens'] == 1024; assert 'agent.architect_pre_plan.max_tokens' in _RANGES; print('settings_store OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/utils/debug_logger.py contains `"ArchitectPrePlan": "agents/architect_pre_plan"`
    - backend/utils/debug_logger.py contains `"ArchitectPrePlanAgent": "agents/architect_pre_plan"`
    - backend/utils/settings_store.py contains `"agent.architect_pre_plan.model": "claude-sonnet-4-5"`
    - backend/utils/settings_store.py contains `"agent.architect_pre_plan.max_tokens": 1024`
    - backend/utils/settings_store.py _RANGES contains `"agent.architect_pre_plan.max_tokens": (512, 4096)`
    - Full test suite still passes: `python3 -m pytest tests/ -v`
  </acceptance_criteria>
  <done>ArchitectPrePlanAgent registered in debug_logger component map and settings_store defaults/ranges</done>
</task>

</tasks>

<verification>
1. `cd /Users/stefan/Code/Travelman3/backend && python3 -m pytest tests/test_agents_mock.py -x -v` — all tests pass including 4 new ones
2. `cd /Users/stefan/Code/Travelman3/backend && python3 -m pytest tests/ -v` — full suite green
3. `python3 -c "from agents.architect_pre_plan import ArchitectPrePlanAgent; print('import OK')"` — agent importable
</verification>

<success_criteria>
- ArchitectPrePlanAgent exists with run() returning dict with regions list
- Agent prompt enforces nights budget = total_days - 1, max_drive_hours_per_day limit, potential-based distribution
- Agent registered in debug_logger and settings_store
- 4 new tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/13-architect-pre-plan-for-interactive-flow/13-01-SUMMARY.md`
</output>
