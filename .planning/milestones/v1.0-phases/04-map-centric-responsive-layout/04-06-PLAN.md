---
phase: 04-map-centric-responsive-layout
plan: 06
type: execute
wave: 4
depends_on: [04-04, 04-05]
files_modified: []
autonomous: false
requirements: [UIR-01, UIR-02, UIR-03, UIR-04, UIR-05, UIR-06]
must_haves:
  truths:
    - "All 6 UIR requirements are visually verified in the running application"
    - "Desktop split-panel layout functions correctly"
    - "Mobile layout is usable on a phone browser"
    - "All interactive features work: card clicks, map sync, day expand, click-to-add"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 4 requirements in the running application.

Purpose: Frontend UI changes cannot be verified through automated tests alone. This checkpoint ensures the map-centric layout, stop cards, day timeline, stats bar, bidirectional sync, and mobile responsiveness all work correctly in the browser.
Output: User confirmation that all UIR requirements are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-map-centric-responsive-layout/04-CONTEXT.md
@.planning/phases/04-map-centric-responsive-layout/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite</name>
  <files></files>
  <read_first>
    - backend/tests/test_models.py (verify TravelStop tags/teaser tests exist)
  </read_first>
  <action>
    Run the full backend test suite to confirm no regressions from the TravelStop model changes:
    ```bash
    cd backend && python3 -m pytest tests/ -v
    ```
    All tests must pass. If any fail, investigate and fix before proceeding to visual verification.
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/DetourAI/backend && python3 -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - All backend tests pass (exit code 0)
    - TravelStop tags/teaser tests included in output
  </acceptance_criteria>
  <done>Full backend test suite green. No regressions from Phase 4 model changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 4 requirements</name>
  <files></files>
  <action>
    Present the verification checklist to the user and wait for confirmation.

    Start the app: `cd /Users/stefan/Code/DetourAI && docker compose up --build`
    Open an existing saved travel at `/travel/{id}` in the browser.

    Verification checklist:

    **UIR-01: Desktop Split-Panel**
    1. On desktop (>= 1100px wide): map fixed left (~58%), content scrollable right (~42%)
    2. Switch tabs: map stays visible, no flicker

    **UIR-02: Mobile Responsive**
    3. Mobile viewport (375px): map sticky strip at top (~30vh)
    4. Content scrollable below
    5. Tab bar scrolls horizontally
    6. All touch targets >= 44px

    **UIR-03: Stop Cards**
    7. Stopps tab: 16:9 photo left, info right
    8. Number badge, name, drive time, nights on each card
    9. Tags as colored pills, teaser text visible
    10. Edit icons always visible

    **UIR-04: Day Timeline**
    11. Tage tab: vertical timeline with left-edge line
    12. Click day header: expands/collapses inline

    **UIR-05: Stats Bar**
    13. Uebersicht tab: 4 stat pills (Tage, Stopps, km, Budget)

    **UIR-06: Map Sync + Click-to-Add**
    14. Click map marker: scrolls to card
    15. Scroll cards: map auto-pans
    16. Click empty map: reverse geocode popup
    17. Sidebar overlay toggle works
  </action>
  <verify>User confirms "approved" or reports issues</verify>
  <done>User has verified all 6 UIR requirements in the running application.</done>
</task>

</tasks>

<verification>
- All backend tests pass
- All 6 UIR requirements verified visually
</verification>

<success_criteria>
- User confirms all requirements met
- No blocking visual or functional issues
</success_criteria>

<output>
After completion, create `.planning/phases/04-map-centric-responsive-layout/04-06-SUMMARY.md`
</output>
