# Phase 3: Route Editing - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-25
**Phase:** 03-route-editing
**Areas discussed:** Editing Scope, Stop Removal Cascade, Custom Stop Input, Metric Recalculation
**Mode:** Auto (--auto flag, recommended defaults selected)

---

## Editing Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Saved travels only (SQLite) | Edit controls on the post-planning guide view, where replace-stop already works | ✓ |
| Route-builder phase (Redis) | Add edit controls during the interactive AI route-building stream | |
| Both | Edit in both phases | |

**User's choice:** [auto] Saved travels only (SQLite) — recommended default
**Notes:** Existing replace-stop infrastructure works on saved travels. Route-builder edits would require managing complex SSE streaming state and Redis job mutations. Saved-travel-first is lower risk and builds on proven patterns.

---

## Stop Removal Cascade

| Option | Description | Selected |
|--------|-------------|----------|
| Reconnect + drop orphaned data | Recalculate A→C directions, drop removed stop's research, update arrival days | ✓ |
| Reconnect + archive data | Keep removed stop's data in a "trash" for potential undo | |
| Soft delete (hide only) | Mark stop as hidden but keep in data model | |

**User's choice:** [auto] Reconnect + drop orphaned data — recommended default
**Notes:** Clean approach. Research data can be regenerated by re-adding the stop. Undo/archive adds complexity without clear user need.

| Option | Description | Selected |
|--------|-------------|----------|
| Confirmation dialog | "Stopp entfernen?" before executing | ✓ |
| No confirmation | Immediate removal | |

**User's choice:** [auto] Confirmation dialog — recommended default
**Notes:** Research data is expensive to regenerate (AI calls). Confirmation prevents accidents.

---

## Custom Stop Input

| Option | Description | Selected |
|--------|-------------|----------|
| Place name text input | User types location name, backend geocodes via Google | ✓ |
| Map click | Click on map to place a stop | |
| Coordinates input | User enters lat/lng directly | |

**User's choice:** [auto] Place name text input — recommended default
**Notes:** Map-click requires Phase 4's map-centric layout. Coordinates are not user-friendly. Text input works with current UI.

| Option | Description | Selected |
|--------|-------------|----------|
| Insert after specific stop | User picks which stop the new one follows | ✓ |
| Drag to position | Drag the new stop into the list | |
| Always append at end | Add to end of route | |

**User's choice:** [auto] Insert after specific stop — recommended default
**Notes:** Simple, explicit. Position can be refined via drag-and-drop reorder afterwards.

| Option | Description | Selected |
|--------|-------------|----------|
| Full research pipeline | Geocode + Directions + Activities + Restaurants + Accommodation + Guide | ✓ |
| Directions only | Just calculate driving segments, skip research | |
| User chooses | Let user decide if they want research | |

**User's choice:** [auto] Full research pipeline — recommended default
**Notes:** A stop without research would be incomplete in the travel plan. Reuse replace_stop_job.py pattern.

---

## Metric Recalculation

| Option | Description | Selected |
|--------|-------------|----------|
| Async via Celery + SSE | Background task with progress streaming | ✓ |
| Synchronous | Block UI until complete | |
| Optimistic + background | Show estimated metrics immediately, refine async | |

**User's choice:** [auto] Async via Celery + SSE — recommended default
**Notes:** Route edits involve Google Directions API calls and AI research. Too slow for synchronous. SSE pattern already proven with replace-stop.

| Option | Description | Selected |
|--------|-------------|----------|
| Full metrics (distance, time, budget, per-stop) | Total km, hours, CHF, plus per-stop breakdown | ✓ |
| Summary only | Just total distance and time | |
| Budget only | Focus on cost impact | |

**User's choice:** [auto] Full metrics — recommended default
**Notes:** Matches existing renderOverview/renderBudget display. Users need the complete picture after edits.

---

## Claude's Discretion

- Celery task structure (per-operation vs. shared)
- Shared recalculation helper extraction
- SSE event naming conventions
- Batch vs. sequential segment recalculation for multi-position reorders
- German wording for UI text

## Deferred Ideas

- Route-builder phase editing (complex SSE state management)
- Map-click stop insertion (requires Phase 4)
- Undo/redo for route edits
- Batch editing (multiple stops at once)
