---
phase: 10-progressive-disclosure-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/maps.js
  - frontend/js/guide-map.js
autonomous: true
requirements: [NAV-05]
must_haves:
  truths:
    - "Non-focused stop markers dim to 0.35 opacity when viewing a specific day or stop"
    - "All markers restore to full opacity on back-navigation to overview"
    - "Map zooms to a specific day's stops region when fitDayStops is called"
  artifacts:
    - path: "frontend/js/maps.js"
      provides: "dimNonFocusedMarkers(), restoreAllMarkers(), fitDayStops() in GoogleMaps namespace"
      exports: ["dimNonFocusedMarkers", "restoreAllMarkers", "fitDayStops"]
    - path: "frontend/js/guide-map.js"
      provides: "Extended _updateMapForTab() with drill-level-aware map focus and dimming"
      contains: "dimNonFocusedMarkers"
  key_links:
    - from: "frontend/js/guide-map.js"
      to: "frontend/js/maps.js"
      via: "GoogleMaps.dimNonFocusedMarkers() and GoogleMaps.restoreAllMarkers() calls"
      pattern: "GoogleMaps\\.dimNonFocusedMarkers"
    - from: "frontend/js/guide-map.js"
      to: "frontend/js/maps.js"
      via: "GoogleMaps.fitDayStops() for day-level map zoom"
      pattern: "GoogleMaps\\.fitDayStops"
---

<objective>
Add map marker dimming and day-scoped map fitting to the GoogleMaps namespace and extend guide-map.js to use drill-level-aware map focus.

Purpose: Implements NAV-05 (marker dimming for non-focused stops) and provides the map focus API needed by Plan 03 when wiring drill-down navigation.
Output: Three new public functions on GoogleMaps (dimNonFocusedMarkers, restoreAllMarkers, fitDayStops) and an updated _updateMapForTab that accepts drill level parameters.
</objective>

<execution_context>
@/Users/stefan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-progressive-disclosure-ui/10-CONTEXT.md
@.planning/phases/10-progressive-disclosure-ui/10-RESEARCH.md
@.planning/phases/10-progressive-disclosure-ui/10-UI-SPEC.md

<interfaces>
From frontend/js/maps.js (GoogleMaps IIFE):
- _guideMarkerList: array of OverlayView markers. Each has m._stopId (string) and m._div (DOM element, may be null if onAdd not yet fired).
- _guidePolylineRef: single polyline for full route. Has setOptions() method.
- _guideMap: the persistent google.maps.Map instance.
- setGuideMarkers(plan, onMarkerClick): clears and recreates all guide markers.
- clearGuideMarkers(): removes all markers from map.
- highlightGuideMarker(stopId): adds .selected class to one marker, removes from others.
- fitAllStops(plan): fits _guideMap bounds to show all stops.
- panToStop(plan, stopId): pans map to a specific stop.
- Return object exposes: clearGuideMarkers, setGuideMarkers, highlightGuideMarker, panToStop, fitAllStops, routeMap (getter), guideMap (getter).
- Marker creation at line 646: m._stopId = stopId (string, e.g. "1", "2").
- Marker DOM element at m._div (set by OverlayView.onAdd, initially null).

From frontend/js/guide-map.js:
- _guideMarkers: array (local copy, parallels _guideMarkerList in maps.js but set during _initGuideMap)
- _guideMapInitialized: boolean
- _updateMapForTab(plan, tab): currently just calls GoogleMaps.fitAllStops(plan) regardless of tab
- Called by renderGuide() after content switch at line 126: _updateMapForTab(plan, activeTab)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dimNonFocusedMarkers, restoreAllMarkers, fitDayStops to maps.js</name>
  <files>frontend/js/maps.js</files>
  <read_first>
    - frontend/js/maps.js (full file to understand _guideMarkerList structure, _guideMap, _guidePolylineRef, the return object exposing public API)
    - .planning/phases/10-progressive-disclosure-ui/10-RESEARCH.md (Pattern 3: Map Dimming with OverlayView Markers, Pitfall 1: _div null)
  </read_first>
  <action>
Add three new functions inside the GoogleMaps IIFE, before the return statement. Then add them to the return object.

FUNCTION 1: dimNonFocusedMarkers(focusedStopIds)
  Parameter: focusedStopIds is an array of stop ID strings (e.g. ["1", "3"]).
  Purpose: Sets opacity 0.35 on markers NOT in the focused set, opacity 1.0 on focused markers.

  Implementation:
  - Iterate _guideMarkerList
  - For each marker m:
    - if (!m || !m._div) continue (Pitfall 1: OverlayView _div may be null if onAdd not yet fired)
    - if m._stopId is undefined (start pin, ferry lines): set opacity 0.35
    - else if focusedStopIds includes String(m._stopId): set m._div.style.opacity = '1', m._div.style.transition = 'opacity 0.3s ease'
    - else: set m._div.style.opacity = '0.35', m._div.style.transition = 'opacity 0.3s ease'

FUNCTION 2: restoreAllMarkers()
  Purpose: Restores all markers to opacity 1.0.

  Implementation:
  - Iterate _guideMarkerList
  - For each marker m:
    - if (!m || !m._div) continue
    - m._div.style.transition = 'opacity 0.3s ease'
    - m._div.style.opacity = '1'

FUNCTION 3: fitDayStops(stops)
  Parameter: stops is an array of stop objects with .lat and .lng properties.
  Purpose: Fits the guide map to show only the provided stops with padding.

  Implementation:
  - if (!_guideMap || !stops.length) return
  - const bounds = new google.maps.LatLngBounds()
  - stops.forEach(s => { if (s.lat && s.lng) bounds.extend({ lat: s.lat, lng: s.lng }); })
  - _guideMap.fitBounds(bounds, { top: 48, right: 48, bottom: 48, left: 48 })

Add all three to the return object:
  dimNonFocusedMarkers,
  restoreAllMarkers,
  fitDayStops,
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/Travelman3 && grep -c "dimNonFocusedMarkers" frontend/js/maps.js && grep -c "restoreAllMarkers" frontend/js/maps.js && grep -c "fitDayStops" frontend/js/maps.js && grep "dimNonFocusedMarkers" frontend/js/maps.js | grep -c "return\|function"</automated>
  </verify>
  <acceptance_criteria>
    - maps.js contains function dimNonFocusedMarkers(focusedStopIds) inside the GoogleMaps IIFE
    - maps.js dimNonFocusedMarkers checks if (!m || !m._div) continue for null safety (Pitfall 1)
    - maps.js dimNonFocusedMarkers sets opacity '0.35' on non-focused markers
    - maps.js dimNonFocusedMarkers sets opacity '1' on focused markers
    - maps.js dimNonFocusedMarkers sets transition 'opacity 0.3s ease' on _div
    - maps.js contains function restoreAllMarkers() that sets opacity '1' on all markers
    - maps.js contains function fitDayStops(stops) that calls _guideMap.fitBounds with 48px padding
    - maps.js return object includes dimNonFocusedMarkers, restoreAllMarkers, fitDayStops
  </acceptance_criteria>
  <done>Three new functions (dimNonFocusedMarkers, restoreAllMarkers, fitDayStops) are callable via GoogleMaps namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Extend _updateMapForTab with drill-level parameters</name>
  <files>frontend/js/guide-map.js</files>
  <read_first>
    - frontend/js/guide-map.js (full file to understand _updateMapForTab, _setupGuideMap, _onMarkerClick)
    - frontend/js/guide-days.js (understand _findStopsForDay for getting day stops)
    - frontend/js/maps.js (confirm the three new functions from Task 1 are exposed)
    - .planning/phases/10-progressive-disclosure-ui/10-CONTEXT.md (D-08 through D-11)
  </read_first>
  <action>
Extend _updateMapForTab to accept drill-level context and manage map focus + marker dimming.

Change function signature from:
  function _updateMapForTab(plan, tab)
To:
  function _updateMapForTab(plan, tab, drillLevel, drillContext)

Where drillLevel is 'overview' | 'day' | 'stop' (default 'overview') and drillContext is { dayNum, stopId } (optional).

Implementation:
  - if GoogleMaps is undefined or !_guideMapInitialized: return
  - if drillLevel === 'day' and drillContext and drillContext.dayNum:
      Per D-09: map zooms to day's region, non-focused stops dimmed.
      1. Get dayStops = _findStopsForDay(plan, Number(drillContext.dayNum))
      2. Get focusedIds = dayStops.map(s => String(s.id))
      3. Call GoogleMaps.fitDayStops(dayStops) for map zoom
      4. Call GoogleMaps.dimNonFocusedMarkers(focusedIds) after a 50ms setTimeout (gives OverlayView time to add _div if markers were just created)
  - else if drillLevel === 'stop' and drillContext and drillContext.stopId:
      Per D-10: map pans to stop area, non-focused dimmed further.
      1. Call GoogleMaps.panToStop(plan, drillContext.stopId) for map pan
      2. Call GoogleMaps.dimNonFocusedMarkers([String(drillContext.stopId)]) after 50ms setTimeout
  - else (overview or fallback):
      Per D-08, D-11: all stops shown, full visibility.
      1. Call GoogleMaps.fitAllStops(plan)
      2. Call GoogleMaps.restoreAllMarkers()

Update the call site in guide-core.js renderGuide() (line 126). Currently:
  _updateMapForTab(plan, activeTab);

This will be updated in Plan 03 to pass drill level. For now, the existing call with just 2 args defaults to overview behavior (drillLevel undefined falls through to else branch), so backward compatibility is maintained.

Also export a new helper function _getMapDrillLevel() that Plan 03 can call:
  function _getMapDrillLevel() returns { level, context } based on _activeDayNum and _activeStopId state:
    - if _activeStopId != null: return { level: 'stop', context: { stopId: _activeStopId, dayNum: _activeDayNum } }
    - if _activeDayNum != null: return { level: 'day', context: { dayNum: _activeDayNum } }
    - return { level: 'overview', context: {} }

Wait: _activeStopId and _activeDayNum are in guide-core.js (module-level vars). guide-map.js already reads them (see line 2: "Reads: activeTab/_activeStopId (guide-core.js)"). So the helper can read them directly. But to keep it clean, instead make _updateMapForTab smart: if drillLevel is not passed, infer from _activeStopId/_activeDayNum:

  function _updateMapForTab(plan, tab, drillLevel, drillContext) {
    if (typeof GoogleMaps === 'undefined' || !_guideMapInitialized) return;

    // Infer drill level from state if not explicitly passed
    if (!drillLevel) {
      if (_activeStopId != null) {
        drillLevel = 'stop';
        drillContext = { stopId: _activeStopId, dayNum: _activeDayNum };
      } else if (_activeDayNum != null) {
        drillLevel = 'day';
        drillContext = { dayNum: _activeDayNum };
      } else {
        drillLevel = 'overview';
        drillContext = {};
      }
    }

    if (drillLevel === 'day' && drillContext && drillContext.dayNum) {
      var dayStops = _findStopsForDay(plan, Number(drillContext.dayNum));
      var focusedIds = dayStops.map(function(s) { return String(s.id); });
      GoogleMaps.fitDayStops(dayStops);
      setTimeout(function() { GoogleMaps.dimNonFocusedMarkers(focusedIds); }, 50);
    } else if (drillLevel === 'stop' && drillContext && drillContext.stopId) {
      GoogleMaps.panToStop(plan, drillContext.stopId);
      setTimeout(function() { GoogleMaps.dimNonFocusedMarkers([String(drillContext.stopId)]); }, 50);
    } else {
      GoogleMaps.fitAllStops(plan);
      GoogleMaps.restoreAllMarkers();
    }
  }

This means the existing renderGuide() call _updateMapForTab(plan, activeTab) will automatically use drill-level awareness based on _activeStopId/_activeDayNum state, with no changes needed to the call site until Plan 03 refines navigation flow.
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/Travelman3 && grep -c "dimNonFocusedMarkers" frontend/js/guide-map.js && grep -c "restoreAllMarkers" frontend/js/guide-map.js && grep -c "fitDayStops" frontend/js/guide-map.js && grep "drillLevel" frontend/js/guide-map.js | head -3 && cd backend && python3 -m pytest tests/ -v -x --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - guide-map.js _updateMapForTab accepts 4 parameters: plan, tab, drillLevel, drillContext
    - guide-map.js _updateMapForTab infers drillLevel from _activeStopId/_activeDayNum if not passed
    - guide-map.js calls GoogleMaps.fitDayStops(dayStops) when drillLevel is 'day'
    - guide-map.js calls GoogleMaps.dimNonFocusedMarkers(focusedIds) in setTimeout(fn, 50) for day level
    - guide-map.js calls GoogleMaps.panToStop when drillLevel is 'stop'
    - guide-map.js calls GoogleMaps.dimNonFocusedMarkers with single stop ID for stop level
    - guide-map.js calls GoogleMaps.fitAllStops and GoogleMaps.restoreAllMarkers for overview level
    - Existing renderGuide call site _updateMapForTab(plan, activeTab) still works (backward compatible)
    - Backend tests pass
  </acceptance_criteria>
  <done>Map responds to drill levels: overview shows all stops at full opacity, day level zooms to day region with non-day stops dimmed, stop level pans to stop with others dimmed. All markers restore on overview.</done>
</task>

</tasks>

<verification>
- When _activeDayNum is set and _updateMapForTab runs, map should zoom to that day's stops and dim others
- When _activeStopId is set, map should pan to that stop and dim others
- When both are null (overview), all markers should be full opacity
- Backend tests pass: cd backend && python3 -m pytest tests/ -v -x
</verification>

<success_criteria>
- GoogleMaps.dimNonFocusedMarkers, restoreAllMarkers, fitDayStops are callable
- _updateMapForTab correctly handles all three drill levels
- Null-safety on marker._div is in place (Pitfall 1)
- 50ms delay on dimming after setGuideMarkers (async OverlayView)
</success_criteria>

<output>
After completion, create `.planning/phases/10-progressive-disclosure-ui/10-02-SUMMARY.md`
</output>
