---
phase: 10-progressive-disclosure-ui
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - frontend/js/guide-core.js
  - frontend/js/guide-days.js
  - frontend/js/guide-stops.js
  - frontend/js/router.js
autonomous: false
requirements: [NAV-02, NAV-03, NAV-04, NAV-06]
must_haves:
  truths:
    - "Clicking a day card drills into day detail with crossfade transition and breadcrumb"
    - "Clicking a stop from day view drills into stop detail with crossfade and breadcrumb"
    - "Breadcrumb back-navigation returns to parent level with crossfade"
    - "Browser back/forward buttons navigate drill levels correctly"
    - "Content scrolls to top on every drill transition"
  artifacts:
    - path: "frontend/js/guide-core.js"
      provides: "Day card click handler in delegation, breadcrumb click handler, renderGuide drill-level integration"
      contains: "day-card-v2"
    - path: "frontend/js/guide-days.js"
      provides: "Updated navigateToDay with crossfade and breadcrumb calls"
      contains: "_drillTransition"
    - path: "frontend/js/guide-stops.js"
      provides: "Updated navigateToStop with crossfade and breadcrumb calls"
      contains: "_drillTransition"
    - path: "frontend/js/router.js"
      provides: "Updated activateDayDetail/activateStopDetail to use crossfade"
      contains: "_drillTransition"
  key_links:
    - from: "frontend/js/guide-core.js"
      to: "frontend/js/guide-days.js"
      via: "Day card click in delegation calls navigateToDay(dayNum)"
      pattern: "navigateToDay"
    - from: "frontend/js/guide-days.js"
      to: "frontend/js/guide-core.js"
      via: "navigateToDay calls _drillTransition and _renderBreadcrumb"
      pattern: "_drillTransition"
    - from: "frontend/js/guide-stops.js"
      to: "frontend/js/guide-core.js"
      via: "navigateToStop calls _drillTransition and _renderBreadcrumb"
      pattern: "_drillTransition"
    - from: "frontend/js/router.js"
      to: "frontend/js/guide-core.js"
      via: "activateDayDetail/activateStopDetail use crossfade for popstate navigation"
      pattern: "_drillTransition"
---

<objective>
Wire the drill-down navigation: day card clicks, stop clicks, breadcrumb back-navigation, browser back/forward, and crossfade transitions at every navigation point. Connect map dimming to drill levels.

Purpose: Completes the three-level drill-down system by connecting all navigation paths (in-app clicks, breadcrumb, browser back/forward) to the crossfade helper, breadcrumb renderer, and map focus management built in Plans 01 and 02.
Output: Fully functional drill-down navigation with transitions, breadcrumbs, and map context at every level.
</objective>

<execution_context>
@/Users/stefan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-progressive-disclosure-ui/10-CONTEXT.md
@.planning/phases/10-progressive-disclosure-ui/10-RESEARCH.md
@.planning/phases/10-progressive-disclosure-ui/10-UI-SPEC.md
@.planning/phases/10-progressive-disclosure-ui/10-01-SUMMARY.md
@.planning/phases/10-progressive-disclosure-ui/10-02-SUMMARY.md

<interfaces>
From Plan 01 (guide-core.js):
- _drillTransition(renderFn, afterRenderFn): crossfade helper, guards against rapid nav, scrolls to top
- _renderBreadcrumb(level, plan, dayNum, stopId): renders breadcrumb bar, hides at overview
- _drillTransitionTimer: module-level var for cancelling in-progress transitions
- renderGuide(plan, tab): central dispatch, calls _updateMapForTab at end

From Plan 01 (guide-overview.js):
- renderOverview(plan): returns HTML with day-cards-grid and collapsible
- _initOverviewInteractions(plan): lazy loads thumbnails, wires collapsible toggle

From Plan 02 (maps.js):
- GoogleMaps.dimNonFocusedMarkers(focusedStopIds): dims non-focused markers to 0.35
- GoogleMaps.restoreAllMarkers(): restores all to 1.0
- GoogleMaps.fitDayStops(stops): fits map to day's stops

From Plan 02 (guide-map.js):
- _updateMapForTab(plan, tab, drillLevel, drillContext): infers drill level from _activeStopId/_activeDayNum if not passed

From existing guide-days.js:
- navigateToDay(dayNum): sets _activeDayNum, calls renderGuide(plan, 'days'), pushes URL /days/{n}
- activateDayDetail(dayNum): sets _activeDayNum, calls renderGuide(S.result, 'days')
- renderDayDetail(plan, dayNum): returns HTML string
- _findStopsForDay(plan, dayNum): returns stop objects for a day

From existing guide-stops.js:
- navigateToStop(stopId): sets _activeStopId, calls renderGuide(plan, 'stops'), pushes URL /stops/{id}
- activateStopDetail(stopId): sets _activeStopId, calls renderGuide(S.result, 'stops')
- renderStopDetail(plan, stopId): returns HTML string

From existing guide-core.js delegation (_initGuideDelegation):
- Single click listener on #guide-content
- Handles: .stop-overview-card, .stop-card-row, .stop-detail-back, .stop-nav-prev/next, .stops-sidebar-item, .stop-day-cta, .day-timeline-header, .day-overview-card, .day-detail-back
- Guarded by _guideDelegationReady boolean (runs once)

From existing router.js:
- Route /travel/{id}/stops/{stopId} handler _travelStopDetail: loads travel, calls activateStopDetail
- Route /travel/{id}/days/{n} handler _travelDayDetail: loads travel, calls activateDayDetail
- Router.navigate(path, opts): pushState or replaceState, dispatches unless skipDispatch
- Router.travelPath(id, title): returns URL path like /travel/42-my-trip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire day card clicks, breadcrumb navigation, and update navigateToDay/navigateToStop with crossfade</name>
  <files>frontend/js/guide-core.js, frontend/js/guide-days.js, frontend/js/guide-stops.js</files>
  <read_first>
    - frontend/js/guide-core.js (full file to see _initGuideDelegation, renderGuide, _drillTransition, _renderBreadcrumb from Plan 01)
    - frontend/js/guide-days.js (full file to see navigateToDay, activateDayDetail, renderDayDetail, _findStopsForDay)
    - frontend/js/guide-stops.js (full file to see navigateToStop, activateStopDetail, renderStopDetail)
    - frontend/js/guide-map.js (see _updateMapForTab with drill-level params from Plan 02)
    - .planning/phases/10-progressive-disclosure-ui/10-RESEARCH.md (Pitfall 3: browser back bypasses crossfade, Pitfall 5: day card clicks silently ignored)
    - .planning/phases/10-progressive-disclosure-ui/10-01-SUMMARY.md (confirm Plan 01 artifacts)
    - .planning/phases/10-progressive-disclosure-ui/10-02-SUMMARY.md (confirm Plan 02 artifacts)
  </read_first>
  <action>
A. Add day card click handler to _initGuideDelegation in guide-core.js:

Inside the existing root.addEventListener('click', (e) => { ... }) block in _initGuideDelegation, add a NEW branch for .day-card-v2 clicks. Add it BEFORE the existing .day-overview-card handler (since day-card-v2 is the new overview layout). Per Pitfall 5: MUST be inside the existing delegation block, NOT a new addEventListener.

Add this branch:
  const dayCardV2 = e.target.closest('.day-card-v2');
  if (dayCardV2) {
    const dayNum = dayCardV2.dataset.dayNum;
    if (dayNum) navigateToDay(dayNum);
    return;
  }

Also add breadcrumb click handler in the same delegation block:
  const bcSegment = e.target.closest('.guide-breadcrumb__segment');
  if (bcSegment && !bcSegment.classList.contains('guide-breadcrumb__segment--active')) {
    const navLevel = bcSegment.getAttribute('data-nav-level');
    if (navLevel === 'overview') {
      _activeDayNum = null;
      _activeStopId = null;
      _navigateToOverview();
      return;
    }
    if (navLevel === 'day') {
      const dayNum = bcSegment.getAttribute('data-day-num');
      _activeStopId = null;
      if (dayNum) navigateToDay(dayNum);
      return;
    }
  }

Wait: breadcrumb is OUTSIDE #guide-content (it is a sibling). The delegation on #guide-content will NOT capture breadcrumb clicks. Need a SEPARATE listener on #guide-breadcrumb. Add this after the _guideDelegationReady guard section:

Add a new function _initBreadcrumbDelegation() in guide-core.js:
  let _breadcrumbDelegationReady = false;
  function _initBreadcrumbDelegation() {
    if (_breadcrumbDelegationReady) return;
    _breadcrumbDelegationReady = true;
    const bar = document.getElementById('guide-breadcrumb');
    if (!bar) return;
    bar.addEventListener('click', function(e) {
      const seg = e.target.closest('.guide-breadcrumb__segment');
      if (!seg || seg.classList.contains('guide-breadcrumb__segment--active')) return;
      const navLevel = seg.getAttribute('data-nav-level');
      const plan = S.result;
      if (!plan) return;
      if (navLevel === 'overview') {
        _activeStopId = null;
        _activeDayNum = null;
        _navigateToOverview();
      } else if (navLevel === 'day') {
        const dayNum = seg.getAttribute('data-day-num');
        _activeStopId = null;
        if (dayNum) navigateToDay(Number(dayNum));
      }
    });
  }

Call _initBreadcrumbDelegation() from renderGuide (add it right after _initGuideDelegation calls, or at the start of renderGuide after getting the content element).

Add a _navigateToOverview() helper in guide-core.js:
  function _navigateToOverview() {
    const plan = S.result;
    if (!plan) return;
    _activeDayNum = null;
    _activeStopId = null;
    _drillTransition(
      function() { return renderOverview(plan); },
      function() { _initGuideDelegation(); if (typeof _initOverviewInteractions === 'function') _initOverviewInteractions(plan); }
    );
    _renderBreadcrumb('overview', plan, null, null);
    _updateMapForTab(plan, 'overview', 'overview', {});
    if (plan._saved_travel_id) {
      var title = plan.custom_name || plan.title || '';
      Router.navigate(Router.travelPath(plan._saved_travel_id, title), { skipDispatch: true });
    }
  }

B. Update navigateToDay in guide-days.js to use crossfade:

Replace the current navigateToDay implementation. Instead of calling renderGuide(plan, 'days') directly, use _drillTransition:

  function navigateToDay(dayNum) {
    _activeDayNum = Number(dayNum);
    _activeStopId = null;
    var plan = S.result;
    if (!plan) return;

    _drillTransition(
      function() { return renderDayDetail(plan, Number(dayNum)); },
      function() {
        _initGuideDelegation();
        _initDayDetailMap(plan, Number(dayNum));
      }
    );

    _renderBreadcrumb('day', plan, dayNum, null);
    _updateMapForTab(plan, 'days', 'day', { dayNum: Number(dayNum) });

    if (plan._saved_travel_id) {
      var title = plan.custom_name || plan.title || '';
      var base = Router.travelPath(plan._saved_travel_id, title);
      Router.navigate(base + '/days/' + dayNum, { skipDispatch: true });
    }
  }

Also update navigateToDaysOverview to navigate to overview (since there is no separate "days overview" in the new drill-down model, going back from a day goes to overview):

  function navigateToDaysOverview() {
    _navigateToOverview();
  }

C. Update navigateToStop in guide-stops.js to use crossfade:

  function navigateToStop(stopId) {
    _activeStopId = Number(stopId);
    var plan = S.result;
    if (!plan) return;

    // Determine which day this stop belongs to for breadcrumb
    var stop = (plan.stops || []).find(function(s) { return String(s.id) === String(stopId); });
    if (stop && _activeDayNum == null) {
      _activeDayNum = stop.arrival_day || null;
    }

    _drillTransition(
      function() { return renderStopDetail(plan, Number(stopId)); },
      function() {
        _initGuideDelegation();
        var s = (plan.stops || []).find(function(x) { return String(x.id) === String(stopId); });
        if (s) {
          _initStopMap(s);
          _lazyLoadSingleStopImages(plan, s);
        }
      }
    );

    _renderBreadcrumb('stop', plan, _activeDayNum, stopId);
    _updateMapForTab(plan, 'stops', 'stop', { stopId: Number(stopId), dayNum: _activeDayNum });

    if (plan._saved_travel_id) {
      var title = plan.custom_name || plan.title || '';
      var base = Router.travelPath(plan._saved_travel_id, title);
      Router.navigate(base + '/stops/' + stopId, { skipDispatch: true });
    }
  }

Also update navigateToStopsOverview to go to overview or back to day detail if _activeDayNum is set:

  function navigateToStopsOverview() {
    _activeStopId = null;
    if (_activeDayNum != null) {
      navigateToDay(_activeDayNum);
    } else {
      _navigateToOverview();
    }
  }

D. Update renderGuide() in guide-core.js to call _renderBreadcrumb and _initBreadcrumbDelegation for all tab cases:

At the start of renderGuide, add: _initBreadcrumbDelegation();

For the 'days' case (when _activeDayNum is set and rendering day detail):
  After the existing content rendering, add:
  _renderBreadcrumb('day', plan, _activeDayNum, null);

For the 'stops' case (when _activeStopId is set and rendering stop detail):
  After the existing content rendering, add:
  _renderBreadcrumb('stop', plan, _activeDayNum, _activeStopId);

For all other cases (overview, days overview, stops overview, calendar, budget):
  _renderBreadcrumb('overview', plan, null, null);

This ensures breadcrumb is always correct when renderGuide is called directly (e.g., from router handlers or tab switching).
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/Travelman3 && grep -c "day-card-v2" frontend/js/guide-core.js && grep -c "_initBreadcrumbDelegation" frontend/js/guide-core.js && grep -c "_navigateToOverview" frontend/js/guide-core.js && grep -c "_drillTransition" frontend/js/guide-days.js && grep -c "_renderBreadcrumb" frontend/js/guide-days.js && grep -c "_drillTransition" frontend/js/guide-stops.js && grep -c "_renderBreadcrumb" frontend/js/guide-stops.js && cd backend && python3 -m pytest tests/ -v -x --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - guide-core.js _initGuideDelegation contains e.target.closest('.day-card-v2') branch that calls navigateToDay
    - guide-core.js contains _initBreadcrumbDelegation function with click handler on guide-breadcrumb element
    - guide-core.js contains _navigateToOverview function that calls _drillTransition, _renderBreadcrumb('overview'), _updateMapForTab, and Router.navigate
    - guide-core.js renderGuide calls _initBreadcrumbDelegation()
    - guide-core.js renderGuide calls _renderBreadcrumb for each tab/drill case
    - guide-days.js navigateToDay calls _drillTransition with renderDayDetail
    - guide-days.js navigateToDay calls _renderBreadcrumb('day', plan, dayNum, null)
    - guide-days.js navigateToDay calls _updateMapForTab with 'day' drillLevel
    - guide-stops.js navigateToStop calls _drillTransition with renderStopDetail
    - guide-stops.js navigateToStop calls _renderBreadcrumb('stop', plan, _activeDayNum, stopId)
    - guide-stops.js navigateToStop calls _updateMapForTab with 'stop' drillLevel
    - guide-stops.js navigateToStopsOverview goes to day detail if _activeDayNum set, else to overview
    - Backend tests pass
  </acceptance_criteria>
  <done>All in-app navigation paths (day card click, stop click, breadcrumb clicks, back buttons) use crossfade transitions, update breadcrumbs, and trigger correct map focus.</done>
</task>

<task type="auto">
  <name>Task 2: Update router handlers for browser back/forward with crossfade</name>
  <files>frontend/js/router.js</files>
  <read_first>
    - frontend/js/router.js (full file to understand _travelStopDetail, _travelDayDetail handlers, and the _travel handler for overview)
    - frontend/js/guide-core.js (confirm _drillTransition, _renderBreadcrumb, _navigateToOverview from Task 1)
    - .planning/phases/10-progressive-disclosure-ui/10-RESEARCH.md (Pitfall 3: browser back bypasses crossfade)
  </read_first>
  <action>
Per D-07 and Pitfall 3 from RESEARCH.md: browser back/forward triggers popstate which calls the router handler, which currently calls activateStopDetail/activateDayDetail which call renderGuide directly, bypassing crossfade.

Fix: Update activateDayDetail in guide-days.js and activateStopDetail in guide-stops.js to use _drillTransition instead of calling renderGuide directly. These are called ONLY by router handlers.

A. Update activateDayDetail in guide-days.js:

  function activateDayDetail(dayNum) {
    _activeDayNum = Number(dayNum);
    _activeStopId = null;
    var plan = S.result;
    if (!plan) return;

    _drillTransition(
      function() { return renderDayDetail(plan, Number(dayNum)); },
      function() {
        _initGuideDelegation();
        _initDayDetailMap(plan, Number(dayNum));
      }
    );

    _renderBreadcrumb('day', plan, dayNum, null);
    _updateMapForTab(plan, 'days', 'day', { dayNum: Number(dayNum) });
  }

B. Update activateStopDetail in guide-stops.js:

  function activateStopDetail(stopId) {
    _activeStopId = Number(stopId);
    var plan = S.result;
    if (!plan) return;

    var stop = (plan.stops || []).find(function(s) { return String(s.id) === String(stopId); });
    if (stop && _activeDayNum == null) {
      _activeDayNum = stop.arrival_day || null;
    }

    _drillTransition(
      function() { return renderStopDetail(plan, Number(stopId)); },
      function() {
        _initGuideDelegation();
        var s = (plan.stops || []).find(function(x) { return String(x.id) === String(stopId); });
        if (s) {
          _initStopMap(s);
          _lazyLoadSingleStopImages(plan, s);
        }
      }
    );

    _renderBreadcrumb('stop', plan, _activeDayNum, stopId);
    _updateMapForTab(plan, 'stops', 'stop', { stopId: Number(stopId), dayNum: _activeDayNum });
  }

C. Update the _travel handler in router.js (the overview route):

The existing _travel handler calls showTravelGuide(plan) which calls renderGuide(plan, activeTab). When navigating BACK to overview from a day/stop URL, this needs to also clear drill state and update breadcrumb.

In the _travel handler, after calling showTravelGuide(plan), add:
  _activeDayNum = null;
  _activeStopId = null;
  if (typeof _renderBreadcrumb === 'function') _renderBreadcrumb('overview', S.result, null, null);

Actually, showTravelGuide already calls renderGuide which now calls _renderBreadcrumb('overview') for the overview case (from Task 1). And _updateMapForTab infers overview from null state. So the popstate back to /travel/{id} should work IF activeTab is 'overview'. But activeTab might still be 'days' or 'stops' from the previous drill.

Fix: In the _travel handler, after showTravelGuide(plan), explicitly:
  activeTab = 'overview';
  _activeDayNum = null;
  _activeStopId = null;
  renderGuide(S.result, 'overview');

But showTravelGuide already calls renderGuide(plan, activeTab). The issue is that activeTab may not be 'overview' if we were on days/stops drill. Need to reset activeTab BEFORE showTravelGuide OR pass the tab.

Simplest fix: In the _travel handler in router.js, just before showSection('travel-guide') and showTravelGuide, reset:
  activeTab = 'overview';
  _activeDayNum = null;
  _activeStopId = null;

Then showTravelGuide calls renderGuide(plan, activeTab) which is now 'overview', which renders overview, hides breadcrumb, restores map markers. This handles browser back from /travel/42/days/3 to /travel/42.

D. Update _travelDayDetail handler in router.js:

Currently calls activateDayDetail(dayNum). Since activateDayDetail now uses crossfade (step A above), browser back to a day URL will also get the crossfade. But on initial page load (direct URL access), we need showTravelGuide to run first. The handler already does this (loads plan, shows section, then calls activateDayDetail). Just ensure activateDayDetail is called AFTER showTravelGuide. Looking at the code, the handler calls showSection then activateDayDetail — but does NOT call showTravelGuide if S.result is already loaded. Need to ensure _setupGuideMap is called.

Add to _travelDayDetail, before activateDayDetail(dayNum):
  if (!document.getElementById('guide-map')?.hasChildNodes()) {
    showTravelGuide(S.result);
  } else {
    showSection('travel-guide');
  }

Wait, looking at the existing code more carefully: the handler already has showSection('travel-guide') then activateDayDetail(dayNum). And showTravelGuide might not be needed because _setupGuideMap may already be initialized. The existing handler works — the only change needed is that activateDayDetail now uses crossfade (from step A). No router.js changes needed for these handlers.

Summary of router.js changes:
- In the _travel handler: add activeTab = 'overview', _activeDayNum = null, _activeStopId = null before showTravelGuide(S.result) call
- No changes to _travelDayDetail or _travelStopDetail handlers (they call activateDayDetail/activateStopDetail which are updated in steps A/B)
  </action>
  <verify>
    <automated>cd /Users/stefan/Code/Travelman3 && grep -c "_drillTransition" frontend/js/guide-days.js && grep "_drillTransition" frontend/js/guide-stops.js | grep -c "activateStopDetail" && grep "activeTab.*overview" frontend/js/router.js | head -3 && cd backend && python3 -m pytest tests/ -v -x --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - guide-days.js activateDayDetail calls _drillTransition with renderDayDetail (not renderGuide directly)
    - guide-days.js activateDayDetail calls _renderBreadcrumb and _updateMapForTab with day drill level
    - guide-stops.js activateStopDetail calls _drillTransition with renderStopDetail (not renderGuide directly)
    - guide-stops.js activateStopDetail calls _renderBreadcrumb and _updateMapForTab with stop drill level
    - router.js _travel handler resets activeTab to 'overview' and clears _activeDayNum/_activeStopId
    - Browser back from /travel/42/days/3 to /travel/42 shows overview with all markers restored
    - Backend tests pass
  </acceptance_criteria>
  <done>Browser back/forward buttons navigate drill levels with crossfade transitions, correct breadcrumbs, and appropriate map focus.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify drill-down navigation in browser</name>
  <what-built>Complete three-level drill-down navigation: overview with day cards, day detail, stop detail. Crossfade transitions, breadcrumb bar, map dimming, browser history integration.</what-built>
  <how-to-verify>
    1. Open a saved travel plan in the browser (http://localhost:8000 or Docker URL)
    2. Verify overview shows: trip summary header, day cards grid (3-up on desktop), collapsible "Reisedetails and Analyse" section
    3. Click the collapsible toggle — content should expand/collapse smoothly
    4. Click a day card — content should crossfade to day detail, breadcrumb shows "Uebersicht > Tag N: Title", map zooms to day region, non-day markers dim
    5. Click a stop in the day detail — content crossfades to stop detail, breadcrumb shows "Uebersicht > Tag N > StopName", map pans to stop
    6. Click "Uebersicht" in breadcrumb — crossfades back to overview, breadcrumb hidden, all markers restored
    7. Click a day card again, then click "Tag N" in breadcrumb from stop level — should go back to day detail
    8. Use browser back button from stop detail — should return to day detail with crossfade
    9. Use browser back again — should return to overview
    10. Navigate directly to /travel/{id}/days/3 URL — should show day detail with correct breadcrumb
    11. Check mobile layout: day cards should stack 2-up then 1-up on narrow screens
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
  <files>none</files>
  <action>Human verifies the complete drill-down navigation in browser. No code changes.</action>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>User approves the drill-down navigation flow</done>
</task>

</tasks>

<verification>
- Full drill-down flow works: overview -> day -> stop -> back to day -> back to overview
- Crossfade transitions visible on all navigation transitions (150ms out + 250ms in)
- Breadcrumb updates correctly at each level, clickable segments navigate correctly
- Map zoom/dim/restore responds to drill level changes
- Browser back/forward restores correct drill level
- Content scrolls to top on every transition
- No console errors during rapid clicking (Pitfall 2: transition guard)
- Backend tests pass: cd backend && python3 -m pytest tests/ -v -x
</verification>

<success_criteria>
- User can navigate overview -> day -> stop via clicks and back via breadcrumb/browser back
- Every transition uses crossfade (no instant content swaps)
- Breadcrumb shows correct segments at each level
- Map responds to drill level (zoom, dimming, restore)
- URL reflects drill level (/travel/{id}, /travel/{id}/days/{n}, /travel/{id}/stops/{n})
- Direct URL access to day/stop URLs works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-progressive-disclosure-ui/10-03-SUMMARY.md`
</output>
