---
phase: 11-browser-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-browser-verification/11-UAT.md
autonomous: false
requirements:
  - VRFY-01
must_haves:
  truths:
    - "All 18 pending UI verification items have been tested in a browser"
    - "Each item has a documented pass/fail result with notes"
    - "Any failures are recorded with enough detail to create fix tasks"
  artifacts:
    - path: ".planning/phases/11-browser-verification/11-UAT.md"
      provides: "Complete verification results for all 18 items"
      contains: "total: 18"
  key_links: []
---

<objective>
Verify all 18 pending human-verification UI items from Phases 3, 4, and 10 against the current progressive disclosure travel view in a running browser.

Purpose: These items were flagged as requiring live browser testing during automated verification of their respective phases. The UI has changed significantly (Phase 10 progressive disclosure), so items from earlier phases may be broken. This is the final quality gate before closing v1.1.

Output: 11-UAT.md with pass/fail status for every item, plus detailed failure descriptions for any broken items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-browser-verification/11-CONTEXT.md
@.planning/milestones/v1.0-phases/04-map-centric-responsive-layout/04-VERIFICATION.md
@.planning/milestones/v1.0-phases/03-route-editing/03-VERIFICATION.md
@.planning/phases/10-progressive-disclosure-ui/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification environment and UAT checklist</name>
  <files>.planning/phases/11-browser-verification/11-UAT.md</files>
  <read_first>
    - .planning/phases/11-browser-verification/11-CONTEXT.md
    - .planning/milestones/v1.0-phases/04-map-centric-responsive-layout/04-VERIFICATION.md
    - .planning/milestones/v1.0-phases/03-route-editing/03-VERIFICATION.md
    - .planning/phases/10-progressive-disclosure-ui/10-VERIFICATION.md
  </read_first>
  <action>
1. Ensure the app is running. Try Docker first per D-06:
   `cd /Users/stefan/Code/Travelman3 && docker compose up --build -d`
   If Docker fails, fall back to local dev server:
   `cd /Users/stefan/Code/Travelman3/backend && python3 -m uvicorn main:app --reload --port 8000`
   Verify the app responds: `curl -s http://localhost:8000/health` (or port 80 for Docker).

2. Create `.planning/phases/11-browser-verification/11-UAT.md` with YAML frontmatter (status: in-progress, phase: 11-browser-verification, source list of the 3 VERIFICATION files, started/updated timestamps) and a structured checklist containing ALL 18 items grouped by feature area.

Group the 18 items into these sections:

**A. Progressive Disclosure (Phase 10) — 5 items** (per D-02, re-verify even though checkpoint-approved):
  A1. Three-level drill-down visual flow (overview -> day -> stop with crossfade)
  A2. Map marker dimming and focus (day/stop dim to 35%, overview restores)
  A3. Browser back/forward navigation with crossfade
  A4. Mobile responsive day cards grid (3-up / 2-up / 1-up)
  A5. Collapsible Reisedetails section (chevron rotation, smooth expand/collapse)

**B. Layout and Responsive (Phase 4) — 3 items:**
  B1. Desktop split-panel layout (map 58% fixed left, content 42% scrollable right)
  B2. Mobile responsive layout (map sticky 30vh top, content below, horizontal tab scroll)
  B3. Sidebar overlay toggle (240px slide-out, stop list, click-outside-close, absent on mobile)

**C. Stop Cards and Stats (Phase 4) — 3 items:**
  C1. Stop cards visual rendering (16:9 photo, number badge, tags, teaser, edit icons)
  C2. Day timeline expand/collapse (single-open accordion, animated)
  C3. Stats bar values (4 pills: Tage, Stopps, km, Budget; red on over-budget)

**D. Map Interaction (Phase 4) — 3 items:**
  D1. Map marker click -> card sync (scroll to card, accent border, marker enlarges)
  D2. Card scroll -> map auto-pan (IntersectionObserver, drag suppression)
  D3. Click-to-add popup (reverse geocode, "Stopp hier hinzufuegen?", Escape dismiss)

**E. Route Editing (Phase 3) — 4 items** (per D-03, never browser-tested):
  E1. Remove stop end-to-end (Entfernen button, confirm dialog, stop disappears, metrics update)
  E2. Add stop end-to-end (+ Stopp hinzufuegen, location input, insert position, research runs)
  E3. Drag-and-drop reorder (drag stop card, visual feedback, new order, recalculate)
  E4. Edit controls disabled during active operation (buttons greyed out while SSE active)

Each item must have these fields:
- `test:` (exact test instructions copied from the respective VERIFICATION.md file)
- `expected:` (expected behavior copied from the respective VERIFICATION.md file)
- `origin:` (which phase, e.g. "Phase 10 — 10-VERIFICATION.md")
- `result:` [pending]
- `notes:` (empty string, to be filled during testing)

End with a Summary section containing: total: 18, passed: 0, issues: 0, pending: 18, skipped: 0, blocked: 0

And an empty Gaps section for recording any failures.
  </action>
  <verify>
    <automated>test -f /Users/stefan/Code/Travelman3/.planning/phases/11-browser-verification/11-UAT.md && grep -c "result:" /Users/stefan/Code/Travelman3/.planning/phases/11-browser-verification/11-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 11-UAT.md exists at .planning/phases/11-browser-verification/11-UAT.md
    - File contains exactly 18 `result:` lines (one per verification item)
    - File contains sections A through E with items A1-A5, B1-B3, C1-C3, D1-D3, E1-E4
    - File contains `total: 18` in Summary section
    - App is running (Docker or local dev server) and responding to health check
  </acceptance_criteria>
  <done>UAT checklist with all 18 items created, app running and accessible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of all 18 UI items</name>
  <files>.planning/phases/11-browser-verification/11-UAT.md</files>
  <read_first>
    - .planning/phases/11-browser-verification/11-UAT.md
  </read_first>
  <action>
Present each group of verification items to the user one at a time. For each item, show the test instructions and expected behavior from the UAT checklist, then record the user's pass/fail/skip result with notes.

Work through these groups in order:
- Group A: Progressive Disclosure (5 items) — drill-down, map dimming, back/forward, responsive grid, collapsible
- Group B: Layout and Responsive (3 items) — desktop split-panel, mobile layout, sidebar overlay
- Group C: Stop Cards and Stats (3 items) — stop cards, day timeline, stats bar
- Group D: Map Interaction (3 items) — marker-card sync, scroll auto-pan, click-to-add
- Group E: Route Editing (4 items) — remove, add, drag-and-drop, edit lock

Per D-07: If a failure is trivial (CSS tweak, missing class, wrong selector), fix it inline and re-verify. If non-trivial (broken SSE flow, missing endpoint, logic error), document it in the Gaps section of 11-UAT.md.

Per D-08: If Phase 10 progressive disclosure broke any Phase 3/4 items, mark these as regressions in the notes — they take priority in any fix plan.

After all items tested, update 11-UAT.md with final results and correct summary counts.
  </action>
  <what-built>
A structured UAT checklist covering all 18 pending UI verification items from Phases 3, 4, and 10. The app is running and ready for browser testing.

Pre-requisite: You need a saved travel plan to test against. Use the test trip (Liestal to Franzoesische Alpen to Paris) or any existing saved travel at /travels.

Testing URL:
- Docker: http://localhost (port 80)
- Local dev: http://localhost:8000
  </what-built>
  <how-to-verify>
Work through the 18 items in the UAT checklist (11-UAT.md), presenting each group to the user for manual browser testing. For each item the user reports pass, fail (with description), or skip.
  </how-to-verify>
  <resume-signal>Report results for each group, or type "all passed" if everything works. For failures, describe what you observed vs what was expected.</resume-signal>
  <verify>
    <automated>grep -c "result: pass\|result: fail\|result: skip" /Users/stefan/Code/Travelman3/.planning/phases/11-browser-verification/11-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 11-UAT.md contains pass/fail/skip result for all 18 items (no remaining "pending")
    - 11-UAT.md Summary section has updated counts matching actual results
    - Any trivial fixes applied inline are committed with fix(11) prefix
    - Any non-trivial failures documented with reproduction steps in 11-UAT.md Gaps section
  </acceptance_criteria>
  <done>All 18 UI items tested in browser. Pass/fail results documented. Trivial fixes applied. Non-trivial failures documented for separate fix plan if needed.</done>
</task>

</tasks>

<verification>
- 11-UAT.md exists with all 18 items tested and documented
- Summary counts match actual pass/fail/skip totals
- Any inline fixes committed with fix(11) prefix
- If all items pass: phase is complete
- If non-trivial failures exist: gap closure plan needed via /gsd:plan-phase 11 --gaps
</verification>

<success_criteria>
- All 18 pending UI verification items tested in browser
- Results documented in 11-UAT.md with pass/fail/skip per item
- Trivial fixes applied and committed
- Non-trivial failures clearly documented for follow-up
- VRFY-01 requirement addressed (verified in browser and fixed if broken)
</success_criteria>

<output>
After completion, create `.planning/phases/11-browser-verification/11-01-SUMMARY.md`
</output>
