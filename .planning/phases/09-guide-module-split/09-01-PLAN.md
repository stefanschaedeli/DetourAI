---
phase: 09-guide-module-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/guide-core.js
  - frontend/js/guide-overview.js
  - frontend/js/guide-stops.js
  - frontend/js/guide-days.js
  - frontend/js/guide-map.js
  - frontend/js/guide-edit.js
  - frontend/js/guide-share.js
autonomous: true
requirements: [STRC-01]

must_haves:
  truths:
    - "guide.js functions are distributed across 7 focused module files"
    - "Every function from guide.js exists in exactly one new module"
    - "Module-level variables are owned by the correct module per D-03"
  artifacts:
    - path: "frontend/js/guide-core.js"
      provides: "Entry point, tab switching, stats bar, cache loading, event delegation"
      contains: "showTravelGuide"
    - path: "frontend/js/guide-overview.js"
      provides: "Overview tab rendering — trip analysis, prose, budget, further activities"
      contains: "renderOverview"
    - path: "frontend/js/guide-stops.js"
      provides: "Stop card rendering, stop detail, stop navigation"
      contains: "renderStopCard"
    - path: "frontend/js/guide-days.js"
      provides: "Day overview, day detail, calendar, time blocks, accommodation/activity/restaurant HTML"
      contains: "renderDaysOverview"
    - path: "frontend/js/guide-map.js"
      provides: "Persistent guide map init, marker clicks, scroll sync, entity images"
      contains: "_initGuideMap"
    - path: "frontend/js/guide-edit.js"
      provides: "All route editing (add/remove/reorder/replace/drag) + SSE edit handlers"
      contains: "_lockEditing"
    - path: "frontend/js/guide-share.js"
      provides: "Share toggle, share link copy"
      contains: "_renderShareToggle"
  key_links:
    - from: "frontend/js/guide-edit.js"
      to: "frontend/js/guide-core.js"
      via: "calls renderGuide() after edit completion"
      pattern: "renderGuide\\("
    - from: "frontend/js/guide-core.js"
      to: "frontend/js/guide-overview.js"
      via: "renderGuide dispatches to renderOverview"
      pattern: "renderOverview\\("
    - from: "frontend/js/guide-core.js"
      to: "frontend/js/guide-stops.js"
      via: "renderGuide dispatches to renderStopsOverview"
      pattern: "renderStopsOverview\\("
    - from: "frontend/js/guide-core.js"
      to: "frontend/js/guide-days.js"
      via: "renderGuide dispatches to renderDaysOverview"
      pattern: "renderDaysOverview\\("
---

<objective>
Extract all functions and variables from the 3010-line guide.js monolith into 7 focused module files.

Purpose: Decompose guide.js per the D-01/D-02 allocation decisions so Phase 10 can work with focused, maintainable modules.
Output: 7 new JS files in frontend/js/ containing all guide.js code, with module header comments per D-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guide-module-split/09-CONTEXT.md
@frontend/js/guide.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract guide-core, guide-overview, guide-stops, guide-days</name>
  <files>frontend/js/guide-core.js, frontend/js/guide-overview.js, frontend/js/guide-stops.js, frontend/js/guide-days.js</files>
  <read_first>frontend/js/guide.js, frontend/js/state.js, frontend/js/maps.js</read_first>
  <action>
Read guide.js fully. Create 4 module files by extracting functions per D-02:

**guide-core.js** (~200 lines):
- Header comment per D-09: "Guide Core — entry point, tab switching, stats, delegation. Reads: S (state.js). Provides: showTravelGuide, renderGuide, switchGuideTab, activateGuideTab, renderStatsBar, loadGuideFromCache, _initGuideDelegation"
- Add `'use strict';` at top
- Module-level variables owned by core per D-03: `let activeTab = 'overview';`, `let _activeStopId = null;`, `let _activeDayNum = null;`
- Extract these functions (preserving exact code): `showTravelGuide` (line 21), `renderGuide` (line 72), `switchGuideTab` (line 145), `activateGuideTab` (line 158), `renderStatsBar` (line 1047), `loadGuideFromCache` (line 2117), `_initGuideDelegation` (line 1309), `replanCurrentTravel` (line 2127)
- Note: `replanCurrentTravel` is called from the replan button set up in `showTravelGuide` and from `_initGuideDelegation` — it belongs with core

**guide-overview.js** (~400 lines):
- Header comment: "Guide Overview — overview tab rendering. Reads: S (state.js), esc() (state.js). Provides: renderOverview, renderTripAnalysis, renderProse, renderTravelGuide, renderFurtherActivities, renderBudget, _lazyLoadOverviewImages, _highlightReqKeywords, _extractReqKeywords, _renderReqTags"
- Add `'use strict';` at top
- No module-level variables
- Extract: `renderOverview` (line 162), `_highlightReqKeywords` (line 229), `_extractReqKeywords` (line 241), `_renderReqTags` (line 257), `renderTripAnalysis` (line 298), `renderProse` (line 377), `renderTravelGuide` (line 382), `renderFurtherActivities` (line 405), `renderBudget` (line 2067), `_lazyLoadOverviewImages` (line 2025)

**guide-stops.js** (~500 lines):
- Header comment: "Guide Stops — stop cards, stop detail, stop navigation. Reads: S (state.js), esc() (state.js), activeTab/_activeStopId (guide-core.js). Provides: renderStopCard, renderStopsOverview, renderStopDetail, navigateToStop, navigateToStopsOverview, activateStopDetail, _onCardClick, _lazyLoadCardImages, _lazyLoadSingleStopImages, _initStopMap, _buildStopMapPin, _buildStopMapPopup"
- Add `'use strict';` at top
- Module-level variable per D-03: `let _initializedStopMaps = new Set();`
- Extract: `renderStopCard` (line 1072), `renderStopsOverview` (line 1140), `_onCardClick` (line 1156), `_lazyLoadCardImages` (line 1173), `renderStopDetail` (line 1190), `navigateToStop` (line 1275), `navigateToStopsOverview` (line 1288), `activateStopDetail` (line 1301), `_initStopMap` (line 1889), `_buildStopMapPin` (line 1835), `_buildStopMapPopup` (line 1842), `_scrollToAndHighlight` (line 1877), `_lazyLoadSingleStopImages` (line 2033)

**guide-days.js** (~700 lines):
- Header comment: "Guide Days — day overview, day detail, calendar, time blocks. Reads: S (state.js), esc() (state.js), activeTab/_activeDayNum (guide-core.js). Provides: renderDaysOverview, renderDayDetail, navigateToDay, navigateToDaysOverview, activateDayDetail, _initDayDetailMap, _toggleDayExpand, _findStopsForDay, renderDayTimeBlocks, _renderAccommodationHtml, _renderActivitiesHtml, _renderRestaurantsHtml, _renderDayExamplesHtml, renderCalendar, _initCalendarClicks"
- Add `'use strict';` at top
- No module-level variables (day state uses _activeDayNum from core)
- Extract: `renderDayTimeBlocks` (line 431), `_renderAccommodationHtml` (line 476), `_renderActivitiesHtml` (line 527), `_renderRestaurantsHtml` (line 556), `_renderDayExamplesHtml` (line 582), `_findStopsForDay` (line 623), `renderDaysOverview` (line 631), `_toggleDayExpand` (line 686), `renderDayDetail` (line 711), `navigateToDay` (line 815), `navigateToDaysOverview` (line 827), `activateDayDetail` (line 839), `_initDayDetailMap` (line 848), `renderCalendar` (line 1407), `_initCalendarClicks` (line 1586)

CRITICAL: Copy function bodies EXACTLY — no modifications to logic, variable names, or whitespace patterns. This is a pure structural split, zero behavioral changes.
  </action>
  <verify>
    <automated>wc -l frontend/js/guide-core.js frontend/js/guide-overview.js frontend/js/guide-stops.js frontend/js/guide-days.js && grep -c "^function \|^async function " frontend/js/guide-core.js frontend/js/guide-overview.js frontend/js/guide-stops.js frontend/js/guide-days.js</automated>
  </verify>
  <acceptance_criteria>
    - guide-core.js contains `function showTravelGuide(`, `function renderGuide(`, `function switchGuideTab(`, `function activateGuideTab(`, `function renderStatsBar(`, `function loadGuideFromCache(`, `function _initGuideDelegation(`, `async function replanCurrentTravel(`
    - guide-core.js contains `let activeTab = 'overview';` and `let _activeStopId = null;` and `let _activeDayNum = null;`
    - guide-core.js starts with `'use strict';`
    - guide-overview.js contains `function renderOverview(`, `function renderTripAnalysis(`, `function renderBudget(`
    - guide-overview.js starts with `'use strict';`
    - guide-stops.js contains `function renderStopCard(`, `function renderStopsOverview(`, `function renderStopDetail(`, `function navigateToStop(`
    - guide-stops.js contains `let _initializedStopMaps = new Set();`
    - guide-days.js contains `function renderDaysOverview(`, `function renderDayDetail(`, `function navigateToDay(`, `function renderCalendar(`
    - guide-days.js contains `function _renderAccommodationHtml(` and `function _renderRestaurantsHtml(`
  </acceptance_criteria>
  <done>4 module files exist with all rendering and navigation functions extracted from guide.js, each with header comment and 'use strict'</done>
</task>

<task type="auto">
  <name>Task 2: Extract guide-map, guide-edit, guide-share</name>
  <files>frontend/js/guide-map.js, frontend/js/guide-edit.js, frontend/js/guide-share.js</files>
  <read_first>frontend/js/guide.js, frontend/js/api.js, frontend/js/maps.js</read_first>
  <action>
Continue extraction from guide.js, creating the remaining 3 module files per D-02:

**guide-map.js** (~400 lines):
- Header comment: "Guide Map — persistent map, markers, scroll sync, entity images. Reads: S (state.js), activeTab/_activeStopId (guide-core.js), GoogleMaps (maps.js). Provides: _initGuideMap, _setupGuideMap, _updateMapForTab, _onMarkerClick, _scrollToAndHighlightCard, _initScrollSync, _scrollToGuideStop, _lazyLoadEntityImages, _getActivityIcon"
- Add `'use strict';` at top
- Module-level variables per D-03: `let _guideMarkers = [];`, `let _guidePolyline = null;`, `let _guideMapInitialized = false;`, `let _userInteractingWithMap = false;`, `let _userInteractionTimeout = null;`, `let _lastPannedStopId = null;`, `let _scrollDebounce = null;`, `let _cardObserver = null;`
- Extract: `_scrollToGuideStop` (line 1599), `_initGuideMap` (line 1603), `_setupGuideMap` (line 1682), `_updateMapForTab` (line 1712), `_onMarkerClick` (line 1718), `_scrollToAndHighlightCard` (line 1732), `_initScrollSync` (line 1744), `_lazyLoadEntityImages` (line 1772), `_getActivityIcon` (line 1820)

**guide-edit.js** (~700 lines):
- Header comment: "Guide Edit — route editing (add/remove/reorder/replace/drag), SSE edit handlers. Reads: S (state.js), esc() (state.js), activeTab (guide-core.js), _fetch/_fetchQuiet/openSSE (api.js), GoogleMaps (maps.js). Provides: _lockEditing, _unlockEditing, _confirmRemoveStop, _executeRemoveStop, _openAddStopModal, _executeAddStop, _haversineKm, _onMapClickToAdd, _showClickToAddPopup, _hideClickToAddPopup, _confirmClickToAdd, _doAddStopFromMap, _onStopDragStart, _onStopDragEnd, _onStopDrop, openReplaceStopModal, closeReplaceStopModal, _switchReplaceTab, _showReplaceProgress, _hideReplaceProgress, _doManualReplace, _doSearchReplace, _selectSearchOption, _listenForReplaceComplete"
- Add `'use strict';` at top
- Module-level variables per D-03: `let _editInProgress = false;`, `let _editSSE = null;`, `let _dragStopSourceIndex = null;`
- Extract: `_lockEditing` (line 2208), `_unlockEditing` (line 2221), `_confirmRemoveStop` (line 2238), `_executeRemoveStop` (line 2286), `_openAddStopModal` (line 2324), `_executeAddStop` (line 2423), `_haversineKm` (line 2467), `_onMapClickToAdd` (line 2478), `_showClickToAddPopup` (line 2490), `_hideClickToAddPopup` (line 2545), `_confirmClickToAdd` (line 2559), `_doAddStopFromMap` (line 2617), `_onStopDragStart` (line 2656), `_onStopDragEnd` (line 2663), `_onStopDrop` (line 2667), `openReplaceStopModal` (line 2717), `closeReplaceStopModal` (line 2800), `_switchReplaceTab` (line 2812), `_showReplaceProgress` (line 2818), `_hideReplaceProgress` (line 2825), `_doManualReplace` (line 2830), `_doSearchReplace` (line 2851), `_selectSearchOption` (line 2893), `_listenForReplaceComplete` (line 2905)

**guide-share.js** (~70 lines):
- Header comment: "Guide Share — share toggle, link copy. Reads: S (state.js), esc() (state.js), _fetchQuiet (api.js). Provides: _renderShareToggle, _handleShareToggle, _copyShareLink"
- Add `'use strict';` at top
- No module-level variables
- Extract: `_renderShareToggle` (line 2940), `_handleShareToggle` (line 2960), `_copyShareLink` (line 3002)

CRITICAL: Copy function bodies EXACTLY — no modifications to logic, variable names, or whitespace patterns. This is a pure structural split, zero behavioral changes.
  </action>
  <verify>
    <automated>wc -l frontend/js/guide-map.js frontend/js/guide-edit.js frontend/js/guide-share.js && grep -c "^function \|^async function " frontend/js/guide-map.js frontend/js/guide-edit.js frontend/js/guide-share.js</automated>
  </verify>
  <acceptance_criteria>
    - guide-map.js contains `function _initGuideMap(`, `function _setupGuideMap(`, `function _updateMapForTab(`, `function _onMarkerClick(`, `function _initScrollSync(`
    - guide-map.js contains `let _guideMarkers = [];` and `let _guidePolyline = null;` and `let _guideMapInitialized = false;`
    - guide-map.js starts with `'use strict';`
    - guide-edit.js contains `function _lockEditing(`, `function _unlockEditing(`, `function openReplaceStopModal(`, `function _listenForReplaceComplete(`
    - guide-edit.js contains `let _editInProgress = false;` and `let _editSSE = null;` and `let _dragStopSourceIndex = null;`
    - guide-edit.js starts with `'use strict';`
    - guide-share.js contains `function _renderShareToggle(`, `async function _handleShareToggle(`, `async function _copyShareLink(`
    - guide-share.js starts with `'use strict';`
    - Total function count across all 7 modules equals function count in guide.js (no functions lost or duplicated)
  </acceptance_criteria>
  <done>3 module files exist with all map, editing, and share functions extracted from guide.js, each with header comment and 'use strict'</done>
</task>

</tasks>

<verification>
- Count total functions across all 7 new files matches 82 functions from guide.js
- Every function name from guide.js appears in exactly one new module
- Each module starts with 'use strict' and has a header comment
- No module contains logic from another module's domain per D-02
</verification>

<success_criteria>
- 7 new JS files created in frontend/js/
- All 82 functions from guide.js distributed per the D-02 allocation
- Module-level variables owned by correct modules per D-03
- Header comments on each file per D-09
- Zero code modifications — only file boundaries changed
</success_criteria>

<output>
After completion, create `.planning/phases/09-guide-module-split/09-01-SUMMARY.md`
</output>
